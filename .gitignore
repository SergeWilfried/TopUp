node_modules/
dist/
.expo/
.wrangler/
.DS_Store
*.log
.dev.vars
# Local env overrides. A tracked .env.local is loaded by production builds too,
# so committing one silently overrides the CI value.
.env.local
.env.*.local

# Generated by `expo prebuild` — regenerate rather than commit. Also keeps the
# test keystore and its password out of the repo.
apps/mobile/android/
apps/mobile/ios/
*.keystore
*.jks
