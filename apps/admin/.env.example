# Base URL of the topup-api Worker. Baked into the bundle at build time, so a
# change here needs a rebuild and redeploy, not just a restart.
# Local development falls back to http://localhost:8787 when this is unset.
VITE_API_URL=https://topup-api.example.workers.dev
